/*@license FCCEEncorder-production V2
 *@license fccev2.cjs.min.js
 *CJS Version.
 *F69's Scriptng License
 *copyright(c) 2026 F69_Scripting. All Rights Reserved.
 *DISCLAIMER
 *There is no guarantee that this code is error-free, bug-free, always works properly, will not be subject to specification changes or service outages, is legal, or can be used with various entities. 
 *This code is provided "AS IS". By using this library, you agree to the license.
 *if you copy this program, add content 'this code is COPY OF FCCEV2"  below  'fccev2_license.txt' and add "copyright(c) [YEAR(do not use "'YY". please use "YYYY")] [NAME]' in copyright section(don't include placeholder(text between "[" and "]"))
 */
class FCCEEncoder { static version = 2; l = "abcdefghik"; staticl = "abcdefghik"; c = 0; FCCEEncoderError = class extends Error { isAPICreated = true; isRetryable = false; constructor(...args) { super(...args); this.name = "FCCEEncoderError"; }; }; getKey(input) { if (!input) return 0; let keypart = input.split("o")[1], key = 0; if (this.staticl.indexOf(keypart) === -1) { key = 0; } else { key = this.staticl.indexOf(keypart); }; return key; }; encode(input, key = 0) { let currentl = this.l; if (!input) throw new this.FCCEEncoderError("input is empty"); if (key >= 10) { key -= Math.floor(key / 10) * 10; }; for (var countraw = 0; countraw < key; countraw++) { currentl = currentl.slice(1) + currentl[0]; }; let res = input.split("").map(t => t.charCodeAt()).map((t, i) => { let n = String(t).split("").map(t => Number(t)); return n.map(t => currentl[t]).join(i % 2 ? "m" : "n"); }).join("l"); let keyModule = key === 0 ? "" : ("o" + this.staticl[key]); return (res.length % 3 === 0 ? res : res + "-".repeat(3 - (res.length % 3))) + keyModule; }; decode(inputr) { ; let prefix = "failed decode FCCE:"; if (!inputr) throw new this.FCCEEncoderError(prefix + "Input is Empty."); let it = typeof inputr; let errTxT = "Input argument [#] type expected 'string' but argument [#] is "; let isArr = Array.isArray(inputr); if (it != "string" && !isArr) throw new this.FCCEEncoderError(prefix + errTxT.replaceAll("#", "0") + it); if (isArr && inputr.length <= 0) { throw new this.FCCEEncoderError(prefix + "Array is empty."); }; let input, key; inputr = (isArr ? inputr[0] : inputr); let mode = inputr.includes("o") ? 2 : 1; if (mode === 2) { input = inputr.split("o")[0]; key = this.getKey(inputr); } else { input = inputr; key = 0; }; let currentl = this.staticl; for (var countraw = 0; countraw < key; countraw++) { currentl = currentl.slice(1) + currentl[0]; }; if (input.length % 3 != 0) { let needpadding = 3 - (input.length % 3); let paddingtext = needpadding === 1 ? "one" : "two"; let padding = "-".repeat(needpadding); let isava = input.length - 6 > 0; let inpsliced = isava ? input.slice(input.length - 6, input.length) : input; let ex = (isava ? "..." : "") + inpsliced + padding; throw new this.FCCEEncoderError(prefix + "FCCE Code Length Invalid.Did you forget " + paddingtext + " '-' for padding? Exapmle:" + ex); }; let res = input.replaceAll("-", "").split("l").map((t, p) => { let s = t.split(p % 2 ? "m" : "n"); let i = s.map(k => currentl.indexOf(k) != -1 ? currentl.indexOf(k) : 0); let r = i.map(t => String(t)).join(""); if (Number.isNaN(Number(r))) throw new this.FCCEEncoderError(prefix + "FCCE Code Invalid.Internal Text(for dev):" + String(r)); return String.fromCharCode(r); }).join(""); if (!res || [0, 65535].includes(res.charCodeAt())) { throw new this.FCCEEncoderError(prefix + "FCCE Code Invalid.Please Check decodeFCCE's argument. "); }; return res; } };module.exports={FCCEEncoder};
